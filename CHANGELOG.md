# 更新日志

## [1.1.0] - 2026-09-15

### 新增

- 主题控制：支持「跟随系统 / 浅色 / 深色」三种模式，选择结果持久化保存，默认跟随系统外观
- 应用列表改为自适应网格布局：图标在上、名称在下，列数随窗口宽度自动增减，单击单元即选中
- 网格单元悬停高亮与提示气泡（显示应用完整路径）
- 应用图标内存缓存，避免滚动列表时重复解析图标

### 变更

- 界面全面适配明暗模式，原先硬编码的黑白配色改为随系统外观变化
- 左侧拖放卡片改为纵向撑满剩余空间，外圆高度与右侧应用列表面板保持一致
- 构建与发布流程迁移至 `xcode-27` 运行器，项目设置升级到 Xcode 27 工具链（部署目标仍保持 macOS 14.0）

### 优化

- 已安装应用列表改为后台队列枚举，并在遍历时跳过包内容与隐藏文件，显著加快列表加载
- 图标改为按需懒加载：单元进入可视区后才在后台解析

### 修复

- 修复 CI 构建产物路径错误：`xcodebuild -derivedDataPath Build` 的产物位于 `Build/Build/Products/<配置>/`，此前校验与制品上传路径少一层目录，导致找不到应用包
- 修复发布 Release 后 `.github/workflows/release.yml` 不自动打包：新增 `release: types: [published]` 触发器，网页或 API 发布 Release 也能触发构建
- 修复发布工作流中版本号解析会强行剥离 `v` 前缀、DMG 步骤引用不存在的构建路径、以及会凭空创建 `v<版本>` 标签等连带问题
- 应用名称过长时最多换行显示 3 行（此前会被截断为单行加省略号），同一行单元高度保持对齐

## [1.0.0] - 2026-05-03

### 新增

- 隔离属性移除工具基础功能：移除应用（`.app`）的 `com.apple.quarantine` 扩展属性
- 拖放支持：可直接将应用拖入界面处理
- 应用列表自动检测已安装的应用，支持搜索与多选
- 批量处理与实时状态反馈
- Liquid Glass 风格半透明界面
- GitHub Actions 持续集成：构建 Debug 产物并上传构建制品

[1.1.0]: https://github.com/HegeKen/QuarantineRemover/compare/r01...HEAD
[1.0.0]: https://github.com/HegeKen/QuarantineRemover/releases/tag/Release1
